# Copy to `.env` (which is gitignored) to pass environment variables into the
# Docker container. All keys are optional.

# Weights & Biases API key for experiment tracking during CLIP/SigLIP training.
# Leave unset to train without W&B logging (pass --no-wandb / WANDB=0).
WANDB_API_KEY=
